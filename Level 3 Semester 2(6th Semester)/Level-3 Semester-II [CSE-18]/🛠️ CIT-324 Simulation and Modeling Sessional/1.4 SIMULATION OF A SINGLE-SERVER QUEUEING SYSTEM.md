The sources provide a detailed explanation of how to simulate a single-server queueing system, including the logic, organization, and a C program implementation, but do not provide a Python implementation. However, the sources contain all the information needed to implement the simulation in Python.

Here's a breakdown of how to approach the implementation, drawing from the provided information:

**1. System Description and Objectives**

*   The system is a single-server queue where customers arrive, wait in a queue if the server is busy, and then receive service.
*   **Interarrival times and service times are independent and identically distributed (IID) random variables**. Specifically, the example uses exponential distributions for both.
*   The goal is to **simulate the system until a fixed number (n) of customers have completed their delays in queue**. The simulation will then estimate the average delay in queue, average number in queue, and server utilization.

**2. Key Components for the Python Implementation**

*   **Simulation Clock:**  A variable to keep track of simulated time.
*   **Event List:** A data structure (e.g., a list or priority queue) to store future events, ordered by their scheduled time.
    *   Events will include customer arrival and service completion (departure).
*   **State Variables:** Variables representing the current state of the system.
    *   Server status (idle or busy).
    *   Number of customers in the queue.
    *   Time of arrival of each customer in the queue.
*   **Statistical Counters:** Variables to accumulate data for performance measures.
    *   Total delay in queue.
    *   Area under the queue length curve.
    *   Area under the server busy curve.
*   **Random Number Generation:** Functions to generate random interarrival and service times from exponential distributions.

**3. Implementation Logic**

*   **Initialization:**
    *   Set the simulation clock to 0.
    *   Initialize the server status to idle, queue length to 0, and the statistical counters to 0.
    *   Schedule the first arrival event using the interarrival time.
*   **Main Simulation Loop:**
    *   The loop continues until the desired number of customers have completed their delay in queue.
    *   **Timing Routine:** Find the next event to occur by scanning the event list.
    *   Advance the simulation clock to the time of the next event.
    *   **Update Time-Average Statistics:** Update the area under the Q(t) curve (number in queue) and area under the B(t) curve (server busy). The area is calculated by multiplying the previous value of Q(t) or B(t) by the time since the last event.
    *   **Event Handling:** Based on the type of event, call the appropriate event routine:
        *   **Arrival Event:**
            *   If the server is idle:
                *   The customer enters service immediately, delay is 0.
                *   Set server status to busy.
                *   Schedule a departure event.
            *   If the server is busy:
                *   Add the customer to the end of the queue, recording arrival time.
            *   Schedule the next arrival event.
        *   **Departure Event:**
            *   If the queue is empty:
                *   Set server status to idle.
            *   If the queue is not empty:
                *   Remove the first customer from the queue.
                *   Calculate and record the delay in queue.
                *   Schedule the next departure event.
*   **Report Generation:**
    *   Calculate the average delay in queue, average number in queue, and server utilization based on accumulated statistics.

**4. Python Code Outline (Conceptual)**

```python
import random
import math

# Parameters
MEAN_INTERARRIVAL = 1.0
MEAN_SERVICE = 0.5
NUM_DELAYS_REQUIRED = 1000

# State variables
sim_time = 0.0
server_status = 0  # 0 for idle, 1 for busy
num_in_q = 0
time_arrival = []
time_last_event = 0.0
num_custs_delayed = 0
total_of_delays = 0.0
area_num_in_q = 0.0
area_server_status = 0.0
next_event_type = 0
time_next_event = [0,0,float('inf')]
num_events = 2


# Random variate generation (exponential)
def expon(mean):
    u = random.random()
    return -mean * math.log(u)

# Event list management (using a simple list)
def timing():
    min_time_next_event = float('inf')
    next_event_type = 0
    for i in range(1,num_events + 1):
      if(time_next_event[i] < min_time_next_event):
        min_time_next_event = time_next_event[i]
        next_event_type = i
    if (next_event_type == 0):
        print(f"\nEvent list empty at time {sim_time}")
        exit()
    sim_time = min_time_next_event
    return next_event_type

def update_time_avg_stats():
    global area_num_in_q, area_server_status, time_last_event
    time_since_last_event = sim_time - time_last_event
    area_num_in_q += num_in_q * time_since_last_event
    area_server_status += server_status * time_since_last_event
    time_last_event = sim_time


# Event routines
def arrive():
    global server_status, num_in_q, num_custs_delayed, total_of_delays
    time_next_event = sim_time + expon(MEAN_INTERARRIVAL)
    if server_status == 1:
        num_in_q += 1
        time_arrival.append(sim_time)
    else:
        delay = 0.0
        total_of_delays += delay
        num_custs_delayed += 1
        server_status = 1
        time_next_event = sim_time + expon(MEAN_SERVICE)
        
def depart():
    global server_status, num_in_q, num_custs_delayed, total_of_delays
    if num_in_q == 0:
        server_status = 0
        time_next_event = float('inf')
    else:
        num_in_q -= 1
        delay = sim_time - time_arrival.pop(0)
        total_of_delays += delay
        num_custs_delayed += 1
        time_next_event = sim_time + expon(MEAN_SERVICE)

def report():
    avg_delay = total_of_delays / num_custs_delayed if num_custs_delayed > 0 else 0
    avg_num_in_q = area_num_in_q / sim_time if sim_time > 0 else 0
    server_util = area_server_status / sim_time if sim_time > 0 else 0
    print(f"Average delay in queue: {avg_delay:.3f} minutes")
    print(f"Average number in queue: {avg_num_in_q:.3f}")
    print(f"Server utilization: {server_util:.3f}")
    print(f"Time simulation ended: {sim_time:.3f} minutes")
   
def initialize():
  global sim_time, server_status, num_in_q, time_last_event, num_custs_delayed, total_of_delays, area_num_in_q, area_server_status, time_next_event
  sim_time = 0.0
  server_status = 0
  num_in_q = 0
  time_arrival = []
  time_last_event = 0.0
  num_custs_delayed = 0
  total_of_delays = 0.0
  area_num_in_q = 0.0
  area_server_status = 0.0
  time_next_event = [0,sim_time + expon(MEAN_INTERARRIVAL),float('inf')]


# Main simulation
initialize()
while num_custs_delayed < NUM_DELAYS_REQUIRED:
    next_event = timing()
    update_time_avg_stats()
    if next_event == 1:
        arrive()
    elif next_event == 2:
        depart()
report()
```

**Key Considerations for Your Python Implementation**

*   **Event List:** The provided outline uses a simple list and linear search which is inefficient for large simulations. Consider using a priority queue (e.g., `heapq` in Python) for efficient event management as suggested in the sources.
*   **Random Number Generation:**  The code provides a basic exponential variate generation; consider using Python's `random` library or `numpy` for more advanced distributions and features.
*   **Code Organization:** Break down the code into smaller, reusable functions for better readability.
*   **Flexibility:** Make the code flexible, for instance by using parameters for mean interarrival/service times and the stopping rule.

By following these steps and using the provided code outline, you should be able to implement the single-server queueing system simulation in Python. Remember to thoroughly test and verify your implementation against known results, if possible.
